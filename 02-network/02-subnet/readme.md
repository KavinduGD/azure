# Subnet

- divide a vnet into smaller segments
- unlike in aws, subnet are regional

## Subnet Public access

Much simpler—Azure gives internet access by default.

Steps:

- Create VNet (Virtual Network)
- Create Subnet

👉 That’s it.

No need for Internet Gateway
No need to manually add route (by default)
